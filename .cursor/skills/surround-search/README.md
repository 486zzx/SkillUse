# surround-search

## 标准化目录

- `skill.json`: 标准元数据
- `prompt.md`: 标准提示入口（兼容层）
- `SKILL.md`: 现有 Cursor 技能定义（保留）
- `resources/`: 标准资源目录（新增）
- `scripts/`: 运行脚本目录（入口不变）

## 运行入口

- `scripts/run_surround_search.py`
